   
 
 //fill(255,0, 0);
 //ellipse(rxa, rya, 32,32);
 ////green
 //fill(0,255, 0);
 //ellipse(rxb, ryb, 32,32);
 //////blue
 ////fill(0,0,255);
 ////ellipse(rxc, ryc, 32,32);
 ////yellow
 //fill(100,100, 0);
 //ellipse(rxd, ryd, 32,32);
 ////teal
 //fill(0,100,100);
 //ellipse(rxe, rye, 32,32);
 ////purple
 //fill(100,0, 100);
 //ellipse(rxf, ryf, 32,32);
  

         
         
         //ellipse(minx, y, 10,10);
         //ellipse(x, miny, 10,10);
         //ellipse(maxx, y, 10,10);
         //ellipse(x, maxy, 10,10);
         //red
         //fill(255,0, 0);
         //ellipse(rxa, rya, 10,10);
         ////green
         //fill(0,255, 0);
         //ellipse(rxb, ryb, 10,10);
         
         
  //  float blobHeight = maxy - miny;
  //  float blobWidth = maxx - minx;
  //  int x;
  //  int y;
    
  //  float hx = 0, hy = 0;
  //  float lhx = 0 ,lhy = 0;
  //  float rhx = 0, rhy = 0;
  //  float lfx = 0, lfy = 0;
  //  float rfx = 0, rfy = 0;
    
  //  int count = 0;
    
  //  hy = miny;
   
 
  //  for ( y = 0; y < blobHeight/3; y++) {
  //    for (x = 0; x < blobWidth; x++) {
  //      int offset = x + y * depthWidth;
  //      int blobID = depth[offset];
        
  //      float newx = map(x, 0, 512, -100, 1920);
  //      float newy = map(y, 0, 424, -100, 1080);
        
  //      if (blobID == id) {
  //        count++;
  //        hx += newx;
          
         
          
          
          
          
          
  //        ellipse(blobWidth % 2,hy,20,20);
  //        println(hx,hy);
  //      }
  //    }
  //  } 
    
  //  for ( y = 0; y > blobHeight/3 && y < 2 * blobHeight/3 ; y++) {
  //    for (x = 0; x < blobWidth; x++) {
  //      int offset = x + y * depthWidth;
  //      int blobID = depth[offset];
        
  //      float newx = map(x, 0, 512, -100, 1920);
  //      float newy = map(y, 0, 424, -100, 1080);
        
  //      if (blobID == id) {
  //        count++;
          
  //        //Left Hand
  //        lhx = minx;
  //        minx += newx;   
  //        lhy = newy % count;
          
  //        ellipse(lhx,lhy,20,20);
          
  //        //Right Hand
  //        rhx = maxx;
  //        maxx += newx;    
  //        rhy = newy % count; 
  //        ellipse(rhx, rhy,20,20);
  //    }
  //  }
  //}
      
  //    for ( y = 0; y > 2 * blobHeight/3 ; y++) {
  //    for (x = 0; x < blobWidth; x++) {
  //      int offset = x + y * depthWidth;
  //      int blobID = depth[offset];
        
  //      float newx = map(x, 0, 512, -100, 1920);
  //      float newy = map(y, 0, 424, -100, 1080);
        
  //      if (blobID == id) {
  //        count++;
          
  //         //Left Foot
  //        lfx = minx;
  //        minx += newx;   
  //        lfy = newy % count;
          
  //        ellipse(lfx,lfy,20,20);
          
  //        //Right Foot
  //        rfx = maxx;
  //        maxx += newx;    
  //        rfy = newy % count; 
          
  //        ellipse(rfx, rfy,20,20);
  //    }
  //  }
  //}
